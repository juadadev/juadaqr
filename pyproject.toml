[project]
name = "juadaqr"
version = "0.1.0"
description = "Generate QR code using URL"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
license = { text = "MIT" }

[dependency-groups]
dev = [
    "ruff>=0.15.13",
]
